#mage
prim main() {
  8(0V; 0VI; 0VII; 0VIII; 0IX; 0X; 0XI; 0XII);
  complete 0;
}

void 8(prim a; prim b; prim c; prim d;
       prim e; prim f; prim g; prim h) {
  out(h);
  h mirror a;
  out(h);
  h mirror g;
  out(h);
}
