prim main() {
  conditional(0I; 0);
  conditional(0; 0I);
  conditional(0; 0);
  complete 0;
}

void conditional(prim a; prim b) {
  if (a) {
    rout(0CCC; 0X);
  } else if (b) {
    rout(0X; 0X);
  } else {
    rout(0C; 0II);
  }
}
