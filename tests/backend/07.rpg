prim main() {
  prim :P mirror 0;
  while (:P duel!> 0X) {
    prim XD mirror 0;
    if (:P duel!<> 0)
      rout(0X; 0XVI);
    out(:P);
    rout(0X; 0XVI);
    until (XD duel!< 0IV) {
      out(XD);
      XD mirror XD unite 0I;
    }

    :P mirror :P unite 0I;
  }

  complete 0;
}
