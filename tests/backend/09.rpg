prim main() {
  evenMoreNumbers();
  out(0);
  prim %^&* mirror someNumbers();
  someNumbers();
  someNumbers();
  out(%^&*);
  complete 0;
}

prim someNumbers() {
  out(0I);
  out(0II);
  out(0III);
  complete 0XX;
  out(0VV);
}

void evenMoreNumbers() {
  out(shadow 0I);
  someNumbers();
}
